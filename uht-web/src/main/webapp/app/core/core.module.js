(function (angular) {
    'use strict';

    angular.module('uht.core', [

        /* AngularJS modules */
        'ngResource',

        /* AngularUI modules */

        /* common, reusable blocks */
        'uht.api'

        /* 3rd party modules */

    ]);

}(window.angular));